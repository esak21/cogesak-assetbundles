# This is a Databricks asset bundle definition for dabproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabproject
  uuid: 8949d5f4-cc1c-4844-8bd2-f630ed902838

include:
  - resources/*.yml
  - resources/*/*.yml



targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: dev_${workspace.current_user.short_name}
      source_linked_deployment: false
    workspace:
      host: https://adb-663477288826036.16.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-663477288826036.16.azuredatabricks.net 
      # We explicitly deploy to /Workspace/Users/esakkisankart_gmail.com#ext#@esakkisankartgmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/esakkisankart_gmail.com#ext#@esakkisankartgmail.onmicrosoft.com/prod/kycassets/.bundle/${bundle.name}/${bundle.target}
 
    permissions:
      - user_name: esakkisankart_gmail.com#ext#@esakkisankartgmail.onmicrosoft.com
        level: CAN_MANAGE
